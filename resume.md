# DocPilot 项目简历素材(2026-09 冻结版 · 唯一口径)

> 说明:本项目历经两轮数据域迁移(医学文献 → Python 后端官方开发文档),该演进作为
> "架构分层、知识库可插拔"的佐证。以下数字为门控增强版冻结口径,与仓库 README /
> `data/eval/eval_report.json` 完全同源 —— **不要让简历出现与之冲突的数字**。
> 内容分两个岗位版本:**应用开发主版**(主投方向)与**算法岗切换版**(被算法岗约面时用)。

---

## 一、一句话定位(挑一句放简历抬头/摘要)

- **应用开发版**:DocPilot —— 面向 Python 后端官方开发文档的 Agentic RAG 问答系统:96 页官方文档入库,中文提问、英文文档检索,回答强制带**可点开官方原文**的引用;复杂需求走自研手写 ReAct 多步检索;前后端分离 + Docker 三服务交付。
- **算法岗版**:面向官方技术文档的检索增强问答系统:手写 BM25/RRF 混合检索 + 交叉编码重排,自建 40 条人工核验评估集与可复算评测管道,以"实验→指标→回滚/增强"驱动检索与拒答策略迭代。

---

## 二、【可直接粘贴】应用开发主版(4~5 条 bullet)

1. **独立开发 DocPilot(面向 Python 后端官方文档的 Agentic RAG 问答系统)**:自研抓取 + HTML 解析入库 96 页官方文档 / 12,170 chunks(正文与**代码块独立成段**,中文提问可返回完整代码示例);手写双通道混合检索(自研 BM25 + bge-m3 向量 → RRF 融合 → 交叉编码重排),回答强制携带可点击的官方原文引用,证据不足自动拒答。
2. **手写 ReAct Agent(不依赖 LangChain)**:3 个检索工具 + Pydantic 参数校验 + 四类护栏(最大步数 8 / 连续重复 2 次 / 单问预算 0.5 元 / 工具超时 30s);启发式路由分流"直接答 / 深度调研"(后者成本约 4 倍,仅复杂问题放行)。
3. **会话可靠性改造**:将问答由同步长请求改造为"会话 / 消息 / 任务"三层 SQLite 持久化 + 后台线程执行 + 前端轮询,回答生成中切走、刷新页面(URL 恢复)均不丢任务;用 playwright 驱动真机浏览器完成 **A~H 38 条断言**验收(发送秒建会话、切走不丢、刷新恢复、失败可见、模式持久化)。
4. **建立可复算评估闭环**:40 条人工核验评估集(反向生成 + 按文档划分 dev/test 防泄漏,含 8 条无答案题),test 实测 **Recall@5 0.773 / MRR 0.628 / 引用完整率 0.955**;针对拒答短板引入灰区语义门控,无答案拒答率 **0.375 → 0.625**(人工复核 8/8 条全部正确拒答)且答出率 0.955 未降;单问成本 **0.0037 元**,指标一条命令可复算。
5. **产品化与工程交付**:Next.js 三页(Chat 三栏 + 引用溯源卡片 + 6 步 RAG 链路可视化 / 知识库管理 / 评测看板)+ FastAPI REST(会话、任务、上传)+ Docker Compose 三服务(qdrant/api/web)一键部署,纯 API、CPU 可跑;110 个离线 mock 测试、ruff/mypy/前端构建全绿、50 commits,内含两轮数据域迁移验证架构解耦。

> 篇幅控制:正式简历建议只留 **1/2/4 三条**(或 1/4+5),其余放详细版。

**技术栈关键词(应用开发版)**:
Python · FastAPI · SQLite(自研 BM25 索引)· Qdrant · DeepSeek API · bge-m3 / bge-reranker · Pydantic · Next.js · React · TypeScript · Tailwind / shadcn-ui · Docker Compose · GitHub Actions · pytest · playwright

---

## 三、【可直接粘贴】算法岗切换版(评测/检索算法向,顺序已调整)

1. **检索算法全链路手写实现**:BM25(词频饱和 k1、文档长度归一 b、IDF)+ RRF 倒数排名融合(规避向量余弦分与词频分的**分数域不可比**问题)+ 稠密向量召回与**交叉编码重排**的两阶段架构(粗召回 30 → 精排 20 → 上下文 6);中文问句跨语言检索英文技术文档(query 改写 + 多语言 embedding 双路)。
2. **自建评估集与可复算评测管道**:40 条人工核验集(段落反向生成保证答案可溯源、dev/test 按文档划分防泄漏、LLM-as-judge 辅助过滤);指标含 Recall@5/@10、MRR、nDCG@10,并对**口径分层**(22 条有答案 / 8 条无答案分开统计引用与拒答)。
3. **实验驱动迭代(有对照、有回滚)**:`final_context_k` 6→8 对照实验(指标下降)已回滚;拒答短板分析定位到"语义沾边"区间 → 引入灰区语义门控(rerank ∈ [0.3, 0.6) 由 LLM 判定证据可答性),无答案拒答率 **0.375→0.625**、答出率 0.955 **无新增误伤**(用同一评估集验证)。
4. **Agent 侧算法实现**:手写 ReAct 循环(Thought/Action/Observation、观察回填、失败自纠)+ 启发式路由(成本差约 4 倍的路径分流)+ 护栏体系(步数/重复/预算/超时)+ 证据编号全局递增分配保证多轮检索一致。
5. **可信度工程**:模型行为与自有逻辑**分离测试**(110 个离线 mock 测试)+ 38 条真机 e2e 断言;数据集/预测文件版本化,指标可一条命令复算;单问成本 0.0037 元。

> 算法岗面试若有"模型侧"追问,如实说明:本项目不做训练/微调/RLHF(纯 API 应用层),模型能力来自第三方;我的优势在**检索算法、评测方法论、实验设计与工程闭环**。

**技术栈关键词(算法岗版,前置检索与评测)**:
RAG(混合检索 / BM25 / RRF / embedding / Rerank)· 评测体系(Recall / MRR / nDCG、评估集构建与防泄漏、LLM-as-judge)· ReAct Agent · Python · SQL · FastAPI · Docker · Git / CI

---

## 四、长版 STAR(详细简历 / 求职邮件 / 自我介绍用)

> 独立开发 **DocPilot:面向开发者的官方技术文档问答 Agent**。自建 96 页官方开发文档知识库
> (自研抓取 + HTML 解析,正文层级与**代码块独立成段**),实现双通道混合检索(手写 BM25 +
> bge-m3 向量 + RRF 融合 + 交叉编码重排)与中英跨语言查询;手写 ReAct Agent(不依赖
> LangChain),含步数/重复动作/费用/超时四类护栏与 Pydantic 工具参数校验;回答强制带可溯源
> 引用与完整代码示例,证据不足自动拒答(空证据 / 低相关 / 灰区语义门控三层)。构建 40 条人工
> 评估集(按文档划分防泄漏、20% 无答案)驱动迭代,test 基线:Recall@5 0.773 / MRR 0.628 /
> 引用完整率 0.955 / 无答案拒答 0.625(人工复核 8/8);单问成本 0.0037 元。
> **可靠性**:"会话/消息/任务"三层持久化 + 轮询,回答中切走、刷新不丢,38 条真机断言验收。
> **产品化**:前后端分离 —— Next.js(React/TS/Tailwind/shadcn)三页(Chat 三栏 + 引用溯源
> 卡片 + 6 步 RAG 链路可视化、知识库管理、评测看板)+ FastAPI 后端(会话/任务/上传 REST),
> Docker 三服务(qdrant + api + web)一键部署(纯 API、CPU 可跑、模型零权重),110 个离线
> 测试与 CI 全绿。

---

## 五、量化成果表(数字先说 · 最新口径)

| 项 | 数值 | 说明 |
|---|---|---|
| 语料规模 | 96 页官方开发文档 / 12,170 chunks | FastAPI 50 + Pydantic 12 + SQLAlchemy 14 + Python 20 |
| 检索效果 | Recall@5 0.773 / Recall@10 0.818 / MRR 0.628 / nDCG@10 0.675 | test 30 条(22 条有答案) |
| 引用完整率 | 0.955 | 回答带可点击的官方原文引用 |
| 无答案拒答率 | 0.625(规则口径);人工复核 8/8 正确拒答 | 灰区门控后由 0.375 提升;答出率 0.955 未降 |
| 评估集 | 40 条人工核验(dev 10 / test 30,8 条无答案) | 反向生成 + 按文档划分防泄漏 |
| 单次成本 | 0.0037 元/问(30 条评估共 0.112 元) | 前缀缓存 + 路由 + 预算护栏 |
| 工程 | 110 个离线测试、38 条真机 e2e 断言、ruff/mypy/前端构建全绿、50 commits | 离线 mock 不依赖付费 API;e2e 脚本已入仓库 |
| 代码检索 | HTML 代码块独立成段 | 中文提问返回完整可运行示例 |

---

## 六、面试深挖清单(每条都能展开 2 分钟)

1. **为什么换了两轮数据域?** → 知识库与 RAG/Agent 核心解耦,换域只改数据接入层与文案;架构复用度约 90%。
2. **BM25 与 RRF 原理?** → k1/b 语义与长度归一直觉;1/(k+rank) 融合为何优于加权平均(分数域不可比)。
3. **为什么向量 + BM25 双通道?各自失效场景?** → 语义改写 vs 精确 API 名/代码符号(如 `OAuth2PasswordBearer`);互补后融合。
4. **代码块检索怎么做的?** → HTML 解析时 `<pre>/<code>` 独立成段、保留章节上下文;对比普通 HTML-to-text 把代码黏进段落。
5. **ReAct 循环与护栏?** → Thought/Action/Observation 手写解析 + Pydantic 校验重试;步数/重复/预算/超时"各防一类事故"。
6. **direct vs agentic 路由?** → 启发式(≤60 字无复杂词走直接答);实测 agentic 成本约 4 倍,靠路由控制。
7. **会话可靠性怎么做的?** → 任务状态落 SQLite(状态机)+ 后台线程执行 + 前端轮询;刷新/切走不丢,URL 恢复 running;38 条真机断言。
8. **评估集怎么保证可信?** → 反向生成(段落即答案)、按文档划分 dev/test、LLM-judge 辅助、≥20% 无答案、指标口径分层;一条命令复算。
9. **拒答怎么做、为什么是 0.625?** → 三层闸:空证据 / rerank 低分 / 灰区语义门控;规则口径 0.625 且人工复核 8/8 全部正确拒答(两套口径如实说明)。
10. **为什么 SQLite 当事实来源而不是 Qdrant?** → 可审计、可迁移;Qdrant/BM25 是派生索引可确定性重建(换 embedding → reindex 一条命令)。
11. **换模型 / 换供应商会怎样?** → 模型调用收敛在客户端层,配置一行切换;换 embedding 需重建索引;对话与向量/重排分属两家供应商(价格与单点故障权衡)。

---

## 七、手写能力(差异点,面试官可深挖)

- 手写 BM25(词频饱和 / 长度归一 / 中文与代码 token)与 RRF 融合(不依赖检索库)
- 手写 ReAct 循环(正则解析、Pydantic 工具校验、四类护栏、引用编号全局递增分配)
- 自研 HTML 正文 / 代码块解析器(标准库状态机,代码块独立成段)
- 自研评估集构建器(反向生成 + LLM-judge 过滤 + 文档级划分)
- 自研灰区语义门控(阈值区间 + LLM 可答性判定 + 解析失败保守放行,并用评估验证无新增误伤)

---

## 八、诚实边界(面试主动说明,加分)

- **两套拒答口径**:规则口径 0.625(系统标记拒答比例),人工逐条复核 8/8 全部正确拒答(3 条为模型自答拒、机器标记未覆盖)—— 文档如实写明。
- **指标口径**:MRR 0.628 偏低主因文档段落碎片化、gold 仅记 1 块而合理回答引用多块;Recall@5 0.773 佐证证据可达;已做 `final_context_k` 6→8 实验并回滚。
- **语料取舍**:96/98 页 —— SQLAlchemy 两个 >800KB 参考页 embedding 接口稳定超时,已剔除并同步语料声明。
- **未做**:SSE 流式、工具调用前人工确认(HITL)、引用后自检、多租户/鉴权、任务取消;后台任务在单机进程内(高并发需消息队列)。
- **不做模型训练/微调/RLHF**:项目为纯 API 应用层;若面算法岗,优势在检索算法、评测方法论与实验设计,不冒充模型侧经验。
