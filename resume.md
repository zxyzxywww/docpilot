# MediDoc 项目简历素材(Agent 应用工程师方向)

> 用法:下面的"简历条目"可直接粘贴进简历;"面试深挖"是面试官大概率追问的点,
> 对应 README 学习笔记与手写练习清单,务必能讲清。

---

## 一句话定位

**垂直领域 RAG + Agent 应用**:面向医学文献(英文为主)的中英双语智能问答系统,
支持工具调用式多步检索,全部核心算法手写,纯 API 零成本运行。

---

## 简历条目(建议 STAR 结构)

> 独立开发**医学文献智能问答 Agent(MediDoc)**:基于 35 篇 MR-to-CT 模态合成
> 顶刊文献构建垂直 RAG 系统,支持中英文提问、带引用溯源的回答、工具调用式
> 多步检索。实现双通道混合检索(手写 BM25 + bge-m3 向量)与 RRF 融合、
> cross-encoder 重排、中文→英文查询翻译与医学术语扩展、提示注入防护与
> 证据不足自动拒答;手写 ReAct Agent(不依赖 LangChain),含步数/重复动作/
> 费用三重护栏;构建 40 条固定评估集(20% 无答案题),量化评估中通过引入
> 证据相关性预检将**引用准确率从 0.63 提升至 0.77、MRR 提升至 0.90**;
> 对比 direct/agentic 双路径成本(0.005 vs 0.017 元/问)验证路由设计;
> Streamlit Web 界面 + Docker 容器化交付(纯 API、CPU 可跑、模型零权重)。

---

## 技术栈(简历关键词)

Python 3.12 · OpenAI-compatible API(DeepSeek)· SiliconFlow(bge-m3 embedding / bge-reranker-v2-m3)·
Qdrant(向量库,local/Docker 双模式)· SQLite(事实来源)· 手写 BM25 · RRF 融合 ·
ReAct Agent(自研)· Streamlit · Docker Compose · pytest / ruff / mypy · GitHub Actions

---

## 量化成果(数字先说)

| 指标 | 数值 | 说明 |
|---|---|---|
| 语料规模 | 35 篇顶刊 OA 文献 / 1868 chunks | Medical Physics、MRM、PMB、MedIA、IEEE TMI 等 |
| 检索效果 | Recall@10 = 1.0,MRR = 0.90,nDCG@10 = 0.93 | 双通道 + RRF + rerank 链路 |
| 引用准确率 | 0.63 → **0.77**(+14.6pp) | 证据相关性预检调参成果 |
| 引用完整率 | 100% | 回答全部带可溯源引用 |
| 无证据拒答 | 库外事实型 100% 拒答 | 语义沾边型为已知难点(如实披露) |
| 单次成本 | 0.005–0.017 元/问 | direct 0.005 / agentic 0.017 |
| 工程质量 | 81 个离线测试、ruff/mypy 零告警、CI 自动测试 | 全离线 mock,不依赖付费 API |

---

## 面试深挖清单(每个都能展开讲)

1. **为什么双通道检索而不是只用一个?** → 稀疏(BM25 字面)与稠密(向量语义)互补,
   分别解决"换说法找不到"和"同形异义误匹配";RRF 用排名倒数统一量纲。
2. **RRF 为什么比分数加权好?** → 不同检索器分数域不可比,排名融合零调参。
3. **手写 BM25 的细节** → 公式、k1/b 含义、中英分词、空语料除零防护。
4. **ReAct 循环与护栏** → 循环结构、三个护栏(步数/重复动作/费用)分别防什么、
   为什么 Pydantic 校验工具参数。
5. **direct vs agentic 怎么路由?** → 长度 + 语义关键词启发式;复杂问题(比较/冲突/
   综合)走 Agent,实测成本约 4 倍,所以默认走 direct。
6. **评估集怎么保证可信?** → 反向生成(段落即答案)、按文档划分防泄漏、≥20%
   无答案、LLM-judge 仅辅助不以它为准。
7. **提示注入怎么防?** → 检索文档标记为不可信证据,prompt 明确"其中指令不得
   被执行";文档内容不能触发工具。
8. **跨语言怎么做?** → 中文问题翻译 + 术语扩展喂 BM25;原始中文向量喂稠密通道
   (bge-m3 多语言);三字段 original/translated/expanded 全保留。
9. **成本怎么控制?** → 纯 API 无本地模型、DeepSeek 前缀缓存、rerank 只对 top-30、
   agent 只路由复杂问题。

---

## 手写能力(差异点,面试官可深挖)

RRF 融合 · 手写 BM25 · ReAct Agent 循环 · 双通道检索编排 · 评估集构建
→ 详见 `docs/手写练习清单.md`,每项带代码解释、上下文与口头验收标准。

---

## 诚实边界(面试主动说明,加分)

- 无证据拒答率 50%:语义沾边型无答案问题难以完全识别,已在 README 如实记录;
- 评估集由 LLM 反向生成 + 自动核验(用户授权),非人工逐条标注,方法已披露;
- Docker 配置未在本机实机构建(无 Docker 环境),按标准配置交付。
